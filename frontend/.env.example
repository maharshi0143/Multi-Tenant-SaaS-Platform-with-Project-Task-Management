# Mandatory: The URL where the backend API is accessible from the BROWSER
# Use localhost because the browser is outside the Docker internal network
VITE_API_URL=http://localhost:5000/api

# For production/Docker internal service-to-service communication only:
# VITE_API_URL=http://backend:5000/api